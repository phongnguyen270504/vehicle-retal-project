<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="#">Car Rent</a>
        <div class="d-flex align-items-center flex-wrap">
            <a class="text-white me-3" href="#">Home</a>
            <a class="text-white me-3" href="#">Cars</a>
            <% if(!locals.user) { %>
                <div class="d-flex align-items-center">
                <a class="text-white text-decoration-none" href="/auth/login">Login</a>
                <div class="text-white mx-2">|</div>
                <a class="text-white text-decoration-none" href="/auth/register">Register</a>
            </div>
            <% } else { %>
                <a class="text-white text-decoration-none" href="/auth/logout">Logout</a>
            <% } %>
        </div>
    </div>
    </nav>